{
  "name": "invoice-demo-frontend",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@11.4.2",
  "workspaces": ["apps/*", "apps/wasm/pkg", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2",
    "typescript": "^5"
  }
}
