// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { StornoPosition } from "../../../../frontend/ui/src/generated/StornoPosition";

/**
 * Intent-Command: storno (cancel) an invoice and emit a mirror credit note
 * in a single atomic operation.
 *
 * Replaces the old multi-step stream of `updateInvoiceHeader` + `createInvoice`
 * + N×`addPosition` + `logActivity`. The client wrapper pre-computes the credit
 * note UUID and positions so both the optimistic client apply and the
 * server-authoritative apply are deterministic and idempotent.
 */
export type Storno = { 
/**
 * Original invoice to cancel.
 */
id: string, 
/**
 * Pre-assigned UUID for the new credit-note document.
 */
credit_note_id: string, customer_id: string | null, credit_note_number: string, date_issued: string, date_due: string, notes: string, service_date: string, cash_allowance_pct: number, cash_allowance_days: number, discount_pct: number, payment_method: string, sepa_mandate_id: string | null, currency: string, language: string, project_ref: string, external_id: string, billing_street: string, billing_zip: string, billing_city: string, billing_country: string, shipping_street: string, shipping_zip: string, shipping_city: string, shipping_country: string, 
/**
 * Pre-negated positions for the credit note (IDs pre-assigned by client).
 */
positions: Array<StornoPosition>, 
/**
 * Activity log fields (shared PK keeps client + server inserts idempotent).
 */
activity_id: string, timestamp: string, };
