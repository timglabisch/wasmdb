[package]
name = "wasmdb-debug"
version = "0.1.0"
edition = "2021"

[dependencies]
sync = { workspace = true, features = ["serde"] }
sql-engine = { workspace = true, features = ["serde"] }
serde = { version = "1", features = ["derive"] }
