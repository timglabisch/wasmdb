[package]
name = "tables-storage"
version = "0.1.0"
edition = "2021"

[dependencies]
tables = { workspace = true }
tables-macros = { workspace = true }
borsh = { version = "1", features = ["derive"] }
