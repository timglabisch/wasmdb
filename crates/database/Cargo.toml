[package]
name = "database"
version = "0.1.0"
edition = "2021"

[dependencies]
sql-parser = { path = "../sql-parser" }
sql-engine = { path = "../sql-engine" }

[features]
default = []
borsh = ["sql-engine/borsh"]
serde = ["sql-engine/serde"]
