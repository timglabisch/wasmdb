[package]
name = "database-projection"
version = "0.1.0"
edition = "2021"

[dependencies]
sql-engine = { workspace = true }
database = { workspace = true }

[features]
default = []
borsh = ["sql-engine/borsh", "database/borsh"]
serde = ["sql-engine/serde", "database/serde"]
