[package]
name = "database"
version = "0.1.0"
edition = "2021"

[dependencies]
sql-parser = { workspace = true }
sql-engine = { workspace = true }

[dev-dependencies]
pollster = "0.3"

[features]
default = []
borsh = ["sql-engine/borsh"]
serde = ["sql-engine/serde"]
