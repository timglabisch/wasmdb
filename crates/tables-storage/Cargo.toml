[package]
name = "tables-storage"
version = "0.1.0"
edition = "2021"

[dependencies]
tables = { path = "../tables" }
tables-macros = { path = "../tables-macros" }
borsh = { version = "1", features = ["derive"] }
